# Copy to `.env.local` (gitignored). Used from VS Code terminal / host PHP only — not loaded in Docker (RUNNING_IN_DOCKER / .dockerenv).
# MariaDB reaches your machine as published port 3308 (see docker-compose `db`). `db` hostname only works inside Compose network.
MYSQL_HOST=host.docker.internal
MYSQL_PORT=3306
